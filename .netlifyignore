# Files that should never be deployed to the Netlify CDN.
# This is the primary defence — these files won't be uploaded at all.
# netlify.toml forced-404 redirects act as a secondary safety net.

# Dev / AI working notes
CLAUDE.md
README.md
CODEBASE.md
TODO.md
ROADMAP.md
ARCHITECTURE.md
APP_AUDIT.md
PRODUCT_JOURNEY.md
HANDOFF.md
LINK_AUDIT_REPORT.md
TRACKING_GUIDE.md
BROKEN_LINKS_QUICK_REFERENCE.txt
EQUITYSIGHT_NOTION_BUNDLE.md

# Platform DB migrations (server-side only, never served)
db/

# Dev/admin utility script — uses REDIS_TOKEN directly, never runs in prod
reset-admin-pw.js

# Node regression tests (dev-only; never needed at runtime).
# NOTE: shared/ is deliberately NOT listed — shared/scenario-schema.js will be
# loaded by the browser after the v2 scenario cutover and must stay servable.
tests/

# Dev tooling: Lighthouse CI config
.lighthouserc.json

# Internal audit notes (mention dev tooling; never public)
docs/

# MCP server config + this ignore file itself
.mcp.json
.netlifyignore

# Claude Code on the web — startup hooks, settings, and the auto-installed
# Lighthouse + Chrome tooling. Never deployed.
.claude/

# Local dev/audit harness scripts that wrap the Claude tooling
bin/

# GSC MCP server — local dev tooling only, never served from the CDN.
# Also keeps any service-account key.json well clear of the public site.
mcp/

# Secrets / VCS / package manifests — defensive blocks (none should be in repo,
# but publish = "." means anything present on disk gets uploaded by default)
.env
.env.*
.git
.gitignore
package.json
package-lock.json

# Runtime error log (auto-synced from Redis — contains client stack traces)
ERRORS.json

# Build scripts (server-side only, not needed on CDN)
build.js
build/

# Build-adjacent scripts still at root
fetch-overpass-amenities.js

# Raw / fetched source data (large intermediate files, not needed at runtime)
data/abs-suburbs.json
data/market-current.json
data/overpass-amenities.json
data/blog-fixture.json
data/blog-posts/
data/state-outlines.js
# Note: data/suburbs.json must remain public — admin Suburbs tab fetches it directly

# HTML templates (used at build time only)
templates/

# Logo/OG image generator HTML templates (use React via CDN; source only)
images/*.html

# Node artefacts
node_modules/
images/og-journey-template.html
data/suburb-coords.json
